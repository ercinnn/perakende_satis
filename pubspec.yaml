name: perakende_satis
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+1

environment:
  sdk: ^3.7.2

dependencies:

  supabase: ^2.3.2

  flutter:
    sdk: flutter



  supabase_flutter: ^2.3.1
  flutter_dotenv: ^5.2.1
  provider: ^6.1.1
  uuid: ^3.0.7
  
  
  cupertino_icons: ^1.0.8
  barcode_scan2: ^4.5.0
  excel: ^2.0.1
  universal_html: ^2.2.1 
  path_provider: ^2.0.11 # Dosya yolu almak için
  open_file: ^3.2.1 # Dosyayı açmak için
  file_selector: ^1.0.3
  intl: ^0.18.1
  logger: ^2.0.2

  
  
  




dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^5.0.0



flutter:
  uses-material-design: true
  assets:
    - .env
